/*
    Rust custom data types are formed mainly though two keywords:
    - `struct`: defines a structure (Product type)
    - `enum`: defines an enumeration (Sum type)

    Constants can also be created via the `const` and `static` keywords
*/

fn main() {}
